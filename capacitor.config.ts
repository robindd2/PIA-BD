import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'PIA-EQUIPO_4',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
